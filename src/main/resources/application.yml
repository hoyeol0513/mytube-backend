server:
  port: 8123
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my-tube?serverTimezone=Asia/Seoul
    username: hoyeol
    password: rkdghduf12!
  jpa:
    hibernate:
#      ddl-auto: create # create update none
    show_sql: true
    format_sql: true
    use_sql_comments: true

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
